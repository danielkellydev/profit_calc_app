<%= turbo_frame_tag "content_frame" do %>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="text-2xl font-semibold text-gray-900 mb-6">Settings</h1>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <%= link_to sale_types_path, class: "block", data: { turbo_frame: "content_frame" } do %>
        <div class="bg-white overflow-hidden shadow rounded-lg">
          <div class="px-4 py-5 sm:p-6">
            <h3 class="text-lg leading-6 font-medium text-gray-900">
              Sale Types
            </h3>
            <p class="mt-1 text-sm text-gray-500">
              Manage your sale types
            </p>
          </div>
        </div>
      <% end %>
      
      <!-- Add more setting cards here as needed -->
    </div>
  </div>
<% end %>