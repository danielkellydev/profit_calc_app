<div class="bg-white shadow rounded-lg p-3 sm:p-4">
  <p class="text-xs uppercase tracking-wide text-gray-500"><%= title %></p>
  <p class="mt-1 text-lg sm:text-xl font-bold text-<%= tone %>-600"><%= number_to_currency(value) %></p>
</div>
